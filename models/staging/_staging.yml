version: 2

models:
  - name: stg_example
    description: Minimal model used to verify the project setup.
    columns:
      - name: id
        description: Stable row identifier.
        tests:
          - not_null
          - unique
